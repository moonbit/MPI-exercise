fn 平均数(i1 : Int, i2 : Int) -> Int {
  let i1 = i1
  let i2 = i2

  // 请在此完成练习
  // 请将abort开头的代码行替换为你对i1与i2进行平均数计算的表达式，小心溢出
  0
}

// -----请忽略以下内容-----

test "第二课练习2" {
  @test.eq(平均数(0, 0), 0)!
  @test.eq(平均数(2, 2), 2)!
  @test.eq(平均数(4, 6), 5)!
  @test.eq(平均数(2, 2147483646), 1073741824)!
  @test.eq(平均数(2147483646, 2147483646), 2147483646)!
}
